{
  "name": "ad-sample",
  "version": "1.0.0",
  "description": "Advertisement sample for quick app",
  "engines": {
    "node": ">=8.10"
  },
  "scripts": {
    "start": "hap server --watch",
    "build": "hap build",
    "release": "hap release",
    "server": "hap server",
    "watch": "hap watch",
    "lint": "eslint --format codeframe --fix --ext .ux,.js src/"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^6.8.0",
    "eslint-plugin-ux": "^0.0.4",
    "hap-toolkit": "^1.9.7",
    "less": "^4.1.2",
    "less-loader": "^10.2.0"
  },
  "browserslist": [
    "chrome 65"
  ]
}
